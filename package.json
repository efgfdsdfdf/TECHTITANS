{
  "name": "techtitans",
  "version": "1.0.0",
  "private": true,
  "description": "TechTitans web application with Capacitor Android shell",
  "scripts": {
    "build:web": "node scripts/prepare-capacitor-web.js",
    "cap:sync": "npm run build:web && npx cap sync android",
    "android:debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run cap:sync && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.1",
    "@capacitor/push-notifications": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
